#include <stdio.h>

int main() {
    char ch;
    printf("Enter a single Character " );
    ch = getchar();
    printf("\n ascii code of entered char is %d \n", ch);
    printf("\n entered char is %c \n", ch);
    return 0;
}
